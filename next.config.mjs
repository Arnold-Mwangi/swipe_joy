/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'jsonplaceholder.typicode.com'], // Add any other domains you need
      },
};

export default nextConfig;
